# Tell direnv to use the Nix Flake definition for this environment
use flake

# Optional: If you want to load environment variables from a .env file
# (make sure .env is in .gitignore if it contains secrets!)
# if [ -f .env ]; then
#   source_env .env
# fi
# echo "Loaded .env file"
